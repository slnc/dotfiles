ssh -A -t "$@" 'source ~/.zshenv; source "${ZDOTDIR}/.zshrc"; last=$(tmux display-message -p "#{session_name}" 2>/dev/null || true); tmux attach -t "$last" 2>/dev/null || tmux attach -t "$(tmux ls -F "#{session_name}" 2>/dev/null | tail -n1)" 2>/dev/null || tmux new -A -s default'
